import { Link } from '@inertiajs/react';
import PublicShell from './PublicShell';
const rupiah = (v) => `Rp ${Number(v || 0).toLocaleString('id-ID')}`;
export default function Plans({ plans }) { return <PublicShell title="Paket Santovate CRM"><section className="sv-public-hero"><span>/ SUBSCRIPTION</span><h1>Pilih paket CRM,<br/><em>lalu konfigurasi bisnis.</em></h1><p>Harga akhir = paket subscription + konfigurasi bisnis. Semua demo konfigurasi dapat dilihat sebelum pembayaran.</p></section><section className="sv-plan-grid">{plans.map((p)=><article key={p.id}><small>{p.name}</small><h2>{rupiah(p.monthly_price)}<span>/bulan</span></h2><p>{p.description}</p><div className="sv-plan-meta"><span>{p.user_limit} user</span><span>{Number(p.prospect_limit).toLocaleString('id-ID')} prospect</span></div><ul>{(p.features||[]).map(x=><li key={x}>✓ {x.replaceAll('_',' ')}</li>)}</ul><Link href={`/business-configurations?plan=${p.key}`} className="btn btn-primary">Pilih {p.name}</Link></article>)}</section></PublicShell>; }
